<%= form_for([@task.project, @task]) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :todo %><br />
    <%= f.text_field :todo %>
  </div>

  <div class="field">
    <%= f.label :status %><br />
    <%= f.select :status, :collection => Task.statuses.keys.last(3).to_a %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
